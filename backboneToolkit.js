define(['jquery','_','backbone','blueprint'], function($,_,Backbone) { 
  var Toolkit = Backbone.Collection.extend(_.extend({}, {

    // (Methods and attributes here)

  }));
  Backbone.Toolkit;
});
